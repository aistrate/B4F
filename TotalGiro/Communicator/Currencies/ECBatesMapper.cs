using System;
using System.Collections;
using System.Text;
using B4F.TotalGiro.Dal;

namespace B4F.TotalGiro.Communicator.Currencies
{
    public class ECBatesMapper
    {
        //static public DateTime GetLatestRateDate(IDalSession DataSession)
        //{
        //    //string sQuery = String.Format(@"select max(RateDate) from HistoricalExRates");

        //    IList returnvalues = ((NHibernate.ISession)DataSession).CreateSQLQuery(@"select max(RateDate) from HistoricalExRates",);
        //    //IList returnvalues =  DataSession.GetList(sQuery);
        //    if (returnvalues.Count > 0)
        //        return (DateTime)returnvalues[0];
        //    else
        //        return DateTime.MinValue;
        //}

    }
}
